{
  "name": "kata",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "@vitest/ui": "^3.2.4",
    "vitest": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:file": "vitest run --reporter=verbose",
    "test:calculator": "vitest run katas/calculator/calculator.test.ts --reporter=verbose",
    "test:string-utils": "vitest run katas/string-utils/string-utils.test.ts --reporter=verbose"
  }
}
